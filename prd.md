# 개인 블로그 프로젝트 PRD

## 1. 개요
비개발자 입문자가 직접 만들고 운영하는 개인 블로그. 초기 설정과 유지보수 부담을 최소화하고, 실제로 완성되어 배포되는 것을 최우선 목표로 한다.

## 2. 목표
- 내가 쓴 글을 웹에 올리고 누구나 URL로 볼 수 있게 한다.
- 코드를 GitHub에 올리면 자동으로 배포되는 흐름을 갖춘다.
- 나중에 댓글, 글쓰기 관리자 화면, 검색 등을 추가할 수 있는 구조로 시작한다.

## 3. 목표가 아닌 것 (Non-goals)
- 대규모 트래픽 대응, 서버 스케일링
- 회원가입/로그인, 결제 등 계정 시스템
- 초기 버전에서 DB 기반 글쓰기 관리자 화면 (필요해지면 2단계에서 추가)

## 4. 기술 스택

| 영역 | 선택 | 이유 |
|---|---|---|
| 프론트엔드 | Next.js (React) | 튜토리얼이 많고 라우팅·이미지 최적화가 기본 제공됨 |
| 콘텐츠 저장 | Markdown 파일 (`.md`) | DB 없이 시작 가능, 글 = 파일이라 개념이 단순함 |
| DB (2단계) | Supabase (PostgreSQL) | 글쓰기 관리자 화면·댓글 등이 필요해질 때 GUI로 관리 가능 |
| 버전 관리 | Git + GitHub | 표준 워크플로, GitHub Desktop으로 GUI 사용 가능 |
| 배포 | Vercel | GitHub 연동 시 push마다 자동 배포, Next.js와 호환성 최상 |

## 5. 핵심 기능 (MVP)
1. 글 목록 페이지 (제목, 날짜, 요약 표시)
2. 글 상세 페이지 (Markdown 본문 렌더링)
3. 새 글은 `.md` 파일 추가 + GitHub push로 발행
4. 반응형 레이아웃 (모바일에서도 읽기 가능)

## 6. 향후 확장 아이디어 (MVP 이후)
- Supabase 연동 → 브라우저에서 글쓰기/수정 (파일 편집 없이)
- 댓글 기능
- 카테고리/태그 필터, 검색
- 커스텀 도메인 연결

## 7. 성공 기준
- 실제 배포된 URL에서 글 목록과 상세 페이지를 확인할 수 있다.
- 새 글을 추가해 GitHub에 push하면 별도 조작 없이 Vercel에 반영된다.
